/**
 * Search API v2
 *
 * Currently mirrors v1 functionality. Future versions will add:
 * - Fuzzy matching
 * - Semantic search
 * - Personalized ranking
 *
 * For now, re-exports v1 implementation for API versioning support.
 */

export { GET } from '../route';
