// useEntry hook removed — EntryFlowV2 is the active flow.
// Types preserved for reference if needed.
export type {
  EntryPhase,
  GateStep,
  FieldStep,
  EntryData,
  EntryState,
  UseEntryOptions,
  UseEntryReturn,
  WaitlistSchoolInfo,
} from './useEntry';
